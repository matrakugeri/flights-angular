export interface Flight {
  id: number;
  flightNumber: string;
  origin: string;
  originFullName: string;
  destination: string;
  destinationFullName: string;
  departureTime: string;
  arrivalTime: string;
  status: string;
  title: string;
  airline: string;
}
