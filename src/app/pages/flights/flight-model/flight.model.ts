export interface Flight {
  id: string;
  flightNumber: string;
  origin: string;
  originFullName: string;
  destination: string;
  destinationFullName: string;
  departureTime: string;
  arrivalTime: string;
  status: string;
  title: string;
}
